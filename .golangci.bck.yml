run:
  timeout: 5m
  tests: true

linters:
  enable:
    - bodyclose
    - gocritic
    - goimports
    - gosec
    - misspell
    - revive
    - unconvert
    - unparam
    - whitespace

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
